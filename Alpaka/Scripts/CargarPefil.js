const CargarPerfil = (usuario, contraseña, telefono, direccion) => {
    $(document).ready(() => {
        $("#email").val(usuario);
        $("#contraseña").val(contraseña);
        $("#tel").val(telefono);
        $("#direccion").val(direccion);
    }
        
        )


}